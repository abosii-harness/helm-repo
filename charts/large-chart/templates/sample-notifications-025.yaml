{{- if and .Values.modules.notifications .Values.modules.notifications.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-notifications-025
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: notifications
data:
  module: "notifications"
  sampleId: "25"
  description: "Sample configmap for notifications module - template 25"
{{- end }}
