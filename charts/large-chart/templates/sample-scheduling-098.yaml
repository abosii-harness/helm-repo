{{- if and .Values.modules.scheduling .Values.modules.scheduling.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-scheduling-098
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduling
data:
  module: "scheduling"
  sampleId: "98"
  description: "Sample configmap for scheduling module - template 98"
{{- end }}
