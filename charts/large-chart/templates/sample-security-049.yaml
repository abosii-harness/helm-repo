{{- if and .Values.modules.security .Values.modules.security.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-security-049
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: security
data:
  module: "security"
  sampleId: "49"
  description: "Sample configmap for security module - template 49"
{{- end }}
