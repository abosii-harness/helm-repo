{{- if and .Values.modules.notifications .Values.modules.notifications.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-notifications-075
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: notifications
data:
  module: "notifications"
  sampleId: "75"
  description: "Sample configmap for notifications module - template 75"
{{- end }}
