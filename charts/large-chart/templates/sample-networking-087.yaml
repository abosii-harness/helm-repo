{{- if and .Values.modules.networking .Values.modules.networking.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-networking-087
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: networking
data:
  module: "networking"
  sampleId: "87"
  description: "Sample configmap for networking module - template 87"
{{- end }}
