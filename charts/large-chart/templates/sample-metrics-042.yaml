{{- if and .Values.modules.metrics .Values.modules.metrics.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-metrics-042
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: metrics
data:
  module: "metrics"
  sampleId: "42"
  description: "Sample configmap for metrics module - template 42"
{{- end }}
