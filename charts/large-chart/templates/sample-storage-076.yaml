{{- if and .Values.modules.storage .Values.modules.storage.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-storage-076
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
data:
  module: "storage"
  sampleId: "76"
  description: "Sample configmap for storage module - template 76"
{{- end }}
