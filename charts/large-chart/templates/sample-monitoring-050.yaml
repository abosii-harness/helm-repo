{{- if and .Values.modules.monitoring .Values.modules.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-monitoring-050
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: monitoring
data:
  module: "monitoring"
  sampleId: "50"
  description: "Sample configmap for monitoring module - template 50"
{{- end }}
