{{- if and .Values.modules.scheduling .Values.modules.scheduling.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "large-chart.fullname" . }}-scheduling-108
  labels:
    {{- include "large-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: scheduling
data:
  module: "scheduling"
  sampleId: "108"
  description: "Sample configmap for scheduling module - template 108"
{{- end }}
